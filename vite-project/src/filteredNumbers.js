// іменний експорт
export const filterNumbers = (numbers) => {
    const filterResult = numbers.filter(number => number >= 10)
    console.log(filterResult);
}

export const user = 'Bob'
export const num = [10, 15, 30]

